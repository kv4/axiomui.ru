node_modules
dist
.vite
*.local
.env
bots/.env
*.png
.playwright-mcp
axiomui-full-site.md

.omo
.opencode

# Внутренние бизнес-материалы в этом репозитории не хранятся:
# стратегия, лиды, письма и задачи живут в отдельном внутреннем репозитории.
# Строки ниже — защита от случайного коммита.
growth/
CONTEXT.md
PLAN.md
incoming/

# Заявки, сохранённые на сервере про запас (не для git)
bots/leads.ndjson
